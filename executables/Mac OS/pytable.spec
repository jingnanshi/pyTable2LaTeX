# -*- mode: python -*-

block_cipher = None


a = Analysis(['pytable.py'],
             pathex=['/Users/John/Documents/pyTable2LaTeX/src'],
             binaries=None,
             datas=None,
             hiddenimports=[],
             hookspath=None,
             runtime_hooks=None,
             excludes=None,
             win_no_prefer_redirects=None,
             win_private_assemblies=None,
             cipher=block_cipher)
pyz = PYZ(a.pure, a.zipped_data,
             cipher=block_cipher)
exe = EXE(pyz,
          a.scripts,
          exclude_binaries=True,
          name='pytable',
          debug=False,
          strip=None,
          upx=True,
          console=False )
coll = COLLECT(exe,
               a.binaries,
               a.zipfiles,
               a.datas,
               strip=None,
               upx=True,
               name='pytable')
app = BUNDLE(coll,
             name='pytable.app',
             icon=None,
             bundle_identifier=None)
